import React from "react";
import { Login, AuthHeader } from "../../components";

const LoginPage = () => {
  return (
    <>
      <AuthHeader />
      <Login />
    </>
  );
};

export default LoginPage;
